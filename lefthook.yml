pre-commit:
  parallel: true
  commands:
    biome:
      glob: '*.{js,mjs,ts,jsx,tsx,css,scss}'
      run: pnpm exec biome check --write --staged

post-merge:
  commands:
    update-env:
      run: pnpm dlx vercel env pull
    install-deps:
      run: pnpm i

